\documentclass[conference]{IEEEtran}
\usepackage{gvv-book}
\usepackage{gvv}
%\IEEEoverridecommandlockouts
\usepackage{tabularx}
\usepackage{placeins}
\usepackage{cite}
%\usepackage{enumitem}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{makecell}
\usepackage{listings}
\usepackage{xurl}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{float}
%\usepackage{tcolorbox}

%\usepackage{minted}
%\usepackage{markdown}
%\usepackage[citations,hybrid]{markdown}

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=5pt,
    showspaces=false,
    showstringspaces=false,
    tabsize=2
}
\lstset{style=mystyle}

%\markdownSetup{
%  renderers = {
%    link = {\href{#3}{#1}}
%  }
%}

%\title{High-Precision Temperature Measurement for PT100: A Comparative Model Analysis}
\title{An AI Based High-Precision Industrial Thermometer}

\author{
    \IEEEauthorblockN{
    Puni Aditya, Vivek K. Kumar, 
    Jnanesh Sathisha Karmar, Burra Shreyas Goud, Dr. G.V.V. Sharma}
    \IEEEauthorblockA{Department of Electrical Engineering, \\Indian Institute of Technology Hyderabad,\\ Kandi, India 502284 \\ gadepall@ee.iith.ac.in}
}

\begin{document}
\maketitle

\begin{abstract}
	In this paper, we design and develop an industrial thermometer using the PT100 temperature sensor.   This is done by 
analyzing the suitability of five machine learning (ML) techniques: a quadratic/inverse quadratic Least Squares (LSQ) model, a Stochastic/inverse Stochastic Gradient Descent (SGD) trained model, and a non-linear Random Forest Regressor (RSR) by calibrating them with respect to a commercial pen thermometer.  The calibration process is also automated using Optical Character Recognition (OCR).
Through numerical results, we conclude that the LSQ model is superior to the other models and implement it using the Arduino framework on an Arduino UNO. 


\end{abstract}

\section{Introduction}
Temperature measurement is a fundamental requirement in process control, automotive systems, and instrumentation. Among the various contact temperature sensors available, the Platinum Resistance Temperature Detector (RTD), commonly known as the PT100, is considered the standard for precision thermometry \cite{rtd_theory}.
The PT100, are widely favored in industrial applications for their linearity and stability. 
\cite{paper4}
 investigated the transient (dynamic) behavior of resistance temperature sensors using two experimental techniques: the Plunge Test and the Self-Heating Test. The goal was to identify the sensor's Transfer Function for use in real-time control systems.
 Here, the focus  was only on the dynamic modeling (time delay/response) of the sensor, not the static (steady-state) calibration or non-linearity correction, which is critical for overall accuracy across a wide range. Futher, the above approach required specialized dynamic testing equipment.
\cite{paper1} used an Artificial Neural Network (ANN) to non-linearly calibrate PT100 sensor readings across $-50\degree$C to $150\degree$C. 
%Achieved $>91\%$ accuracy improvement, reducing error from $\sim 1.3\degree$C to $\sim 0.27\degree$C.
In \cite{paper3}, Multisim software was used 
%to model the circuit,
% Focused on 
 for the simulation and design of the measurement transducer. 
 %Used a DC balance Wheatstone bridge configuration and  achieving high simulated accuracy (high $\mathbf{R^2}$ and low MSE) for a $10\degree$C to $90\degree$C range.
Both  the above  works were primarily based on software simulation only. They was  did not implement or compare against machine learning models, relying solely on theoretical circuit performance,
%However, this study was done completely on software, 
leaving the real-time processing feasibility and computational overhead on embedded hardware unaddressed.
    % --- Paper 2: New Circuit Design (376796536) ---
A high-accuracy temperature measurement circuit using a Wheatstone bridge, an AD623 differential amplifier, and an STM32F4 microcontroller was designed and implemented  in \cite{paper2}.
 However, the design relies on expensive, specialized analog components and a high-performance microcontroller. 
 %Critically, there is a \textbf{lack of machine learning and comparative study} against contemporary digital calibration or ML methods.
    %\url{https://www.researchgate.net/publication/386448957_Pt100_temperature_measurement_sensors_design_and_transducer_simulation}
    
    % --- Paper 4: Dynamic Properties Identification (237074979) ---
   % \url{https://www.researchgate.net/publication/237074979_Identification_and_prediction_of_the_dynamic_properties_of_resistance_temperature_sensors}

To address the above issues,   we compare the accuracy of three different regression approaches:  the LSQ quadratic, grounded in the theoretical Callendar-Van Dusen equation \cite{callendar_vd}, the SDG and RSR \cite{scikit-learn}.  The coefficients, mathematical formulations, and validation accuracy for each model are presented, demonstrating that machine learning approaches can augment traditional calibration methods.
In the process, we  detail the calibration and characterization of a PT100 sensor by comparing two hardware signal conditioning setups: a simple voltage divider and a precision Wheatstone bridge interfaced with an ADS1115 ADC.   
We also automate the process of generating the dataset by using OCR to capture the readings of the analog thermometer through a mobile phone camera.
The parameters for all the above ML techniques are finally hardcoded in the ATMEGA328P microcontroller, using the Arduino board.  Through numerical performance plots, we show that LSQ outperforms other ML techniques considered in this paper.
%for the superior Wheatstone bridge data:

\section{PT-100 Fundamentals}
%\subsection{Fundamental Principles}
The PT100 is defined by a nominal resistance of $100\,\Omega$ at $0\degree$C. Unlike thermocouples which generate a voltage, RTDs differ in that they operate by changing electrical resistance in direct proportion to temperature changes \cite{sensor_physics}. This relationship is inherently non-linear over wide ranges, though it is highly predictable. The resistance $R(t)$ at temperature $t$ is governed by the Callendar-Van Dusen equation \cite{callendar_vd}
\begin{equation}
    R(t) = R_0 (1 + A t + B t^2)
\end{equation}
for temperatures $t > 0\degree$C, where $A$ and $B$ are standard coefficients. This quadratic physical relationship serves as the theoretical justification for using LSQ models in this work.

%\subsection{Measurement Challenges}
While PT100 sensors offer high stability, the resistance change is relatively small ($\approx 0.385\,\Omega/\degree$C). Consequently, accurate reading requires precise signal conditioning to convert resistance changes into measurable voltage levels \cite{wheatstone_ref}. This paper aims to optimize this conversion by analyzing different hardware front-ends and software regression models \cite{lsq_method} to find an optimal solution for high-precision, low-cost temperature sensing.

\section{Hardware Setup}
Two main analog front-ends were tested to evaluate the trade-off between circuit complexity and measurement accuracy.
\tabref{table:list_table}
%Table \ref{table:list_table}
lists the components required for both analog front-ends,  detailing the specific parts used for high-accuracy and cost-effective implementations.
%
\input{tables/list_table}
%
\begin{itemize}
    \item {\em High-Precision Wheatstone Bridge with ADC:} This uses a Wheatstone bridge configuration coupled with an external 16-bit ADS1115 ADC \cite{ads1115_datasheet}. This setup allows for differential measurement, significantly reducing noise and common-mode errors.
The connection details for integrating the external 16-bit ADS1115 ADC are  available
%critical for differential measurement accuracy.
in \tabref{table:ads1115_connections}
\input{tables/ads1115_connections}
The complete circuit schematic for the high-precision Wheatstone bridge setup is illustrated in \figref{fig:wheatstone_schematic}.
\begin{figure}[h!]
    \centering
    \includegraphics[width=\columnwidth]{figs/circuit_schematic.png}
    \caption{Circuit Schematic of the High-Precision Wheatstone Bridge (Setup 1)}
    \label{fig:wheatstone_schematic}
\end{figure}
The complete circuit schematic for the simple voltage divider setup is illustrated in Figure \ref{fig:voltage_divider_schematic}.
\begin{figure}[h!]
    \centering
    \includegraphics[width=\columnwidth]{figs/circuit_schematic_2.jpg}
    \caption{Circuit Schematic of the simple voltage divider (Setup 2)}
    \label{fig:voltage_divider_schematic}
\end{figure}
%\subsubsection{ADS1115 ADC Connections}
%
    \item {\em Simple Voltage Divider:} This uses a simple voltage divider utilizing the Arduino's internal 10-bit ADC.
\end{itemize}
The Arduino-LCD connections are available in 
\tabref{table:lcd_connections}.
\input{tables/lcd_connections}
\section{Automated Training Using OCR}
%\markdownInput{AutoTrainer/README.md}
The dataset is collected by simulating a real-world thermal change (cooling cycle) to capture paired temperature readings. Since the training data is available only on an analog thermometer, instead of manually noting the temperature, we automate the data collection process using OCR based techniques.
Appendix \ref{app:AutoTrainer} has more information on the software execution.
\subsection{Experimental Setup}
The experimental setup is available in 
\figref{fig:autotrainer_schematic}.
  The practical requirements are listed below
\begin{enumerate}
	\item {\em Hardware:}
\begin{itemize}
    \item A webcam connected to your computer.
    \item {(Optional)} An NVIDIA GPU with CUDA installed. The script is pre-set to use \texttt{gpu=True} for much faster processing.
\end{itemize}
%
\item {\em Software:}
\begin{itemize}
    \item Python 3.x
    \item The required Python libraries: \texttt{opencv-python}, \texttt{easyocr}, and \texttt{numpy}.
\end{itemize}
\end{enumerate}
%
\begin{figure}[H]
    \centering
    \includegraphics[width=\columnwidth]{figs/autotrainer_schematic.png}
    \caption{Auto-Trainer representation}
    \label{fig:autotrainer_schematic}
\end{figure}

\subsection{Data Collection Methodology}\label{data-collection-methodology}
\begin{enumerate}
    \item {Setup:} Place both the PT100 probe and the reference thermometer inside an electric kettle, submerged in water.
    \item {Heating Phase:} Turn on the kettle and heat the water to its boiling point.
    \item {Recording Phase:} 
    \begin{itemize}
        \item Switch off the kettle at boiling point.
        \item Start recording a {video} using the smartphone.
        \item Ensure {both the PT100 LCD display} and {reference thermometer} are visible in the frame.
    \end{itemize}
    \item {Cooling Phase:} Continue recording the cooling process as the water temperature drops naturally.
    \item {Data Extraction:} The recorded video acts as the raw dataset.
%    \begin{itemize}
        \item Extract frames and apply OCR to detect readings from both displays.
%    \end{itemize}
\end{enumerate}

%\subsection{Component Lists}
%\subsection{Connection Schematics and Diagrams}
%\subsubsection{Circuit Schematic}
%\subsubsection{ADS1115 ADC Connections}
%\subsubsection{LCD Display Connections}
%A standard JHD 162A Parallel Liquid Crystal Display (LCD) was used for real-time temperature output in both setups.
%Component lists and connection diagrams are provided in the appendix tables.
%\section{Models Used}

\section{Machine Lerning Models}
%This report details the development and evaluation of several regression models for predicting sensor output. Specifically, we focus on two scenarios: predicting temperature from voltage using a Wheatstone bridge with ADC, and predicting resistance from voltage using a voltage divider without ADC. 
%As mentioned earlier, we investigate Random Forest Regressor, Inverse Quadratic Least Squares (LSQ), Direct LSQ, and Stochastic Gradient Descent (SGD) approaches \cite{scikit-learn}.

%\subsection{Model Explanations}
The LSQ and SDG models obtained from the dataset derived in the previous section are available in 
    \tabref{tab:model_coefficients}.
The relevant software description for obtaining these models is available in Appendix \ref{app:models} 
	The coefficients are determined through linear least squares regression \cite{lsq_method}.  
\input{tables/model_coefficients.tex}
\begin{enumerate}

\item 	For 
inverse LSQ and SDG
methods, 
to predict $Y$ from a given $X$, we use the quadratic formula
\begin{align}
Y = \frac{{-b \pm \sqrt{{b^2 - 4a(c - X)}}}}{{2a}} 
\end{align}
This method aligns with the physical properties described by the Callendar-Van Dusen equation \cite{callendar_vd}.
The SGD Regressor is a linear model trained with Stochastic Gradient Descent \cite{scikit-learn}. Due to its sensitivity to feature scaling, both the input features (X) and the target variable (Y) were scaled using StandardScaler before training. The predictions were then inverse-transformed back to the original scale for evaluation.
	\iffalse
\item {\em Inverse LSQ (Quadratic) for Voltage Divider without ADC: }
This model approximates the relationship between temperature ($Y$) and voltage ($X$) from the voltage divider circuit as $X = aY^2 + bY + c$. The fitted equation for the Voltage Divider is\begin{align} 
	X = -0.000014990Y^2 + 0.005589496Y + 2.526066163 
\end{align}
 The model predicts resistance ($Y$) from measured voltage ($X$) using the positive root of the quadratic equation: \[ Y = \frac{{-b \pm \sqrt{{b^2 - 4a(c - X)}}}}{{2a}} \]
This method is suitable for circuits where the voltage-resistance relationship can be approximated quadratically.

\item {\em Inverse LSQ (Quadratic) for Wheatstone with ADC: }
This model approaches the Wheatstone bridge problem by fitting a quadratic equation of the form \(X = aY^2 + bY + c\), where Y is temperature and X is voltage. The fitted equation for the Wheatstone bridge is:
\[ X = -0.000006744Y^2 + 0.004344843Y + 0.056019368 \]
To predict Y from a given X, we use the quadratic formula:
\[ Y = \frac{{-b \pm \sqrt{{b^2 - 4a(c - X)}}}}{{2a}} \]
\fi

\item {\em Standard LSQ and SDG: }
This model applies a standard quadratic fit directly mapping voltage to temperature, expressed as 
\begin{align}
Y = aX^2 + bX + c.
\end{align}
 Unlike the inverse models, this approach predicts temperature ($Y$) directly from the voltage ($X$) without requiring the use of the quadratic formula for inversion during the prediction phase.
\iffalse
\begin{itemize}
    \item \textbf{Fitted Equation:} $Y = aX^2 + bX + c$
    \item \textbf{Coefficients:} 
    \begin{align*}
        a=190.226107678\\ b=173.380527679\\ c=-7.072446221
    \end{align*}
\end{itemize}
\fi
%\item {\em Stochastic Gradient Descent (SGD) Regressor (Wheatstone with ADC): }

%\item {\em Inverse SGD Regressor (Wheatstone with ADC): }
%This model utilizes Stochastic Gradient Descent to learn the inverse relationship, where voltage ($X$) is the target and temperature ($Y$) is the feature ($X = f(Y)$). Similar to the Inverse LSQ approach, the model learns to predict voltage from temperature. During inference, the temperature is derived by numerically inverting the learned function.

\item {\em Random Forest Regressor: }
The Random Forest Regressor is an ensemble learning method that constructs a multitude of decision trees during training \cite{scikit-learn}. For regression tasks, it averages the predictions of individual trees to produce a more stable and accurate prediction. This model was applied to predict temperature (Y) from voltage (X) directly, leveraging its ability to capture complex non-linear relationships without explicit feature engineering.
\end{enumerate}
%\begin{table}[H]
 %   \centering
 %   \caption{Model Coefficients}
 %   \label{tab:model_Coefficients}
 %   \input{tables/model_coefficients.tex}
%\end{table}

\section{Model Evaluation and Comparison}

%\subsection{Performance Metrics Summary}

    \tabref{tab:model_performance}
summarizes the Mean Squared Error (MSE) and R-squared (R2) scores for all evaluated models \cite{MSER2}.  Based on these metrics, 
the LSQ (Quadratic) model for the Wheatstone bridge with ADC outperforms the rest.  Therefore,  the voltage divider method without external ADC is not suitable for our design.
    \input{tables/performance}
%\begin{table}[H]
%    \centering
%    \caption{Model Performance Metrics}
%    \label{tab:model_performance}
%\end{table}
%\subsection{Validation Data and Model Predictions (Subset)}
    \iffalse
The below table presents a small subset of test data, showing the actual and predicted temperature values for the Voltage Divider setup.

\begin{table}[h!]
\centering
\caption{Voltage Divider Validation Table with Predictions}
\label{tab:voltage_divider_validation_predictions}
\begin{tabular}{|l|c|c|}
\hline
Voltage & Actual Temperature & Prediction \\
\hline
2.7300 & 39.7 & 40.9900 \\
2.7500 & 46.4 & 45.6500 \\
2.8400 & 68.3 & 68.8900 \\
2.8920 & 85.6 & 84.7100 \\
2.9100 & 90.0 & 90.8000 \\
2.8700 & 77.8 & 77.7400 \\
\hline
\end{tabular}
\end{table}
\fi

\tabref{tab:validation_data_subset}
presents a small subset of the test data, showing the actual temperature values alongside the predictions from the Random Forest, Inverse LSQ, LSQ, SGD, and Inverse SGD Regressor models for the Wheatstone bridge setup with ADC.
The corresponding graph is available in 
\figref{fig:wheatstone_model_comparison}.
%
We thus conclude that the LSQ model is best at predicting the temperature.  Thouge the results are provided only for a small range of temperatures in 
\tabref{tab:validation_data_subset}
and
\figref{fig:wheatstone_model_comparison}
for brevity and visualization respectively,  the results hold true for all values of practical interest.
\begin{table}[H]
\centering
\caption{Wheatstone Validation Data and Predictions}
\label{tab:validation_data_subset}
\resizebox{\linewidth}{!}{%
\input{tables/validation}%
}
\end{table}
%\subsection{Graphical Comparison of Wheatstone ADC Models}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{figs/combined_model_plot.png}
    \caption{Comparison of Random Forest, Inverse LSQ, LSQ, SGD and Inverse SGD Regressor predictions against actual test data for the Wheatstone bridge with ADC for temperatures between $53.5\degree$C to $56.5\degree$C}
    \label{fig:wheatstone_model_comparison}
\end{figure}
%\subsection{Model Comparison and Conclusion}
% --- Bibliography Section ---
\bibliographystyle{IEEEtran}
\bibliography{references}
%\newpage
\appendices

\section{}
\label{app:AutoTrainer}
\input{AutoTrainer/output.tex}
The final training data is in the following text file
\fbox{\parbox{\linewidth}{\url{https://github.com/gadepall/pt100/blob/main/AutoTrainer/trainingdata.txt}
}}

\section{Appendix - Arduino Deployment}
\label{app:models}
The models are obtained and implemented using Python and the scikit-learn\cite{scikit-learn}.
This section presents the evaluation of the trained models and a comparison of their performance based on Mean Squared Error (MSE) and R-squared (R2) metrics.
\ref{app:models}
\fbox{\parbox{\linewidth}{\url{https://github.com/gadepall/pt100/blob/main/codes/models/PT100_models.ipynb}
}}
Following are the steps to compiling the code using \textbf{PlatformIO} and uploading it to the Arduino via \textbf{Arduino IDE}.
\begin{enumerate}
	\item Create and initialize the project. \\
		\begin{lstlisting}[language=bash]
			mkdir pt100_project
			cd pt100_project
			pio project init --board uno
		\end{lstlisting}
	\item The arduino libraries \texttt{Liquid Crystal} and \texttt{Adafruit ADS1115} must be downloaded. \\
		\begin{lstlisting}[language=bash]
			pio lib install "LiquidCrystal"
			pio lib install "Adafruit ADS1X15"
		\end{lstlisting}
    \item Connect Arduino to the computer via USB.
    \item Compile the following code using the given command after you copy the code into \texttt{src/main.cpp} in the \texttt{pt100\_project} folder. \\
		\begin{lstlisting}[language=bash]
			pio run
		\end{lstlisting}
	    %using PlatformIO.
	%{\em Platformio}.
\fbox{\parbox{\linewidth}{\url{https://github.com/gadepall/pt100/blob/main/codes/arduino/code/code.ino}
}} \\
	\item Upload the file \texttt{firmware.hex} from the folder \texttt{.pio/build/uno/firmware.hex} into to the Arduino board using \textbf{Arduino IDE}.
%\item Open PlatformIO, select New paper and then fill in the details (name, board \& framework).
%\item Then replace contents in src/main.cpp with the above code, now run \& upload that code to Arduino Uno.
\end{enumerate}
The Arduino code,
\begin{itemize}
    \item Finds the voltage across the wheatstone bridge setup with the PT100 through the ADC
    \item Predicts the temperature from the voltage with the model.
    \item Displays the temperature on the LCD display.
\end{itemize}


\end{document}
